# This file outlines the frontend environment variables required for running chaptered-web.

# Vite Dev Server Port
PORT=5173

# URL of the Chaptered Express API (if accessing directly bypassing proxy)
# By default, Vite proxies /api requests to http://localhost:3000 via vite.config.ts
VITE_API_URL=http://localhost:3000
